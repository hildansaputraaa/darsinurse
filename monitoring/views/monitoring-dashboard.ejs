<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manajemen Ruangan - Darsinurse</title>
  <link rel="icon" type="image/png" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='0.9em' font-size='90'>🏥</text></svg>">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  
  <style>
    :root {
      --primary-green: #00B14F;
      --secondary-teal: #20c997;
      --primary-blue: #0056b3;
      --danger-red: #dc3545;
      --gradient-primary: linear-gradient(135deg, #00B14F 0%, #20c997 100%);
      --gradient-blue: linear-gradient(135deg, #0d6efd 0%, #0056b3 100%);
      --gradient-bg: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      --shadow-sm: 0 2px 8px rgba(0,0,0,0.08);
      --shadow-md: 0 4px 16px rgba(0,0,0,0.12);
      --shadow-lg: 0 8px 32px rgba(0,0,0,0.15);
      --shadow-xl: 0 12px 48px rgba(0,0,0,0.2);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', 'Segoe UI', -apple-system, BlinkMacSystemFont, sans-serif;
      background: var(--gradient-bg);
      min-height: 100vh;
      position: relative;
      overflow-x: hidden;
    }

    body::before {
      content: '';
      position: fixed;
      top: -50%;
      right: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(0,177,79,0.05) 0%, transparent 70%);
      animation: rotate 60s linear infinite;
      z-index: 0;
      pointer-events: none;
    }

    @keyframes rotate {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }

    .top-header {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      padding: 1.2rem 0;
      box-shadow: 0 4px 20px rgba(0,0,0,0.08);
      position: sticky;
      top: 0;
      z-index: 1000;
      border-bottom: 1px solid rgba(0,177,79,0.1);
    }

    .logo-info {
      display: flex;
      align-items: center;
      gap: 14px;
      transition: transform 0.3s ease;
    }

    .logo-info:hover {
      transform: translateX(5px);
    }

    .logo-info i {
      font-size: 36px;
      background: var(--gradient-primary);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      filter: drop-shadow(0 2px 4px rgba(0,177,79,0.3));
      animation: pulse 2s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }

    .logo-info h1 {
      font-size: 26px;
      font-weight: 800;
      color: #1a3a3a;
      margin: 0;
      letter-spacing: -0.5px;
    }

    .logo-info small {
      display: block;
      font-size: 12px;
      color: #6c757d;
      font-weight: 500;
      margin-top: 2px;
    }

    .user-info {
      display: flex;
      align-items: center;
      gap: 18px;
      font-size: 14px;
      font-weight: 500;
    }

    .user-info span:not(.text-muted) {
      color: #495057;
    }

    .user-info a {
      color: var(--primary-blue);
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      padding: 6px 12px;
      border-radius: 8px;
    }

    .user-info a:hover {
      color: var(--primary-green);
      background: rgba(0,177,79,0.1);
      transform: translateY(-2px);
    }

    .main-container {
      padding: 28px 0;
      position: relative;
      z-index: 1;
    }

    .page-title {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      padding: 22px 26px;
      border-radius: 16px;
      box-shadow: var(--shadow-md);
      margin-bottom: 28px;
      border: 1px solid rgba(255,255,255,0.8);
      position: relative;
      overflow: hidden;
    }

    .page-title::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: var(--gradient-primary);
    }

    .page-title h2 {
      margin: 0;
      color: #1a3a3a;
      display: flex;
      align-items: center;
      gap: 14px;
      font-weight: 800;
      font-size: 24px;
      letter-spacing: -0.5px;
    }

    .page-title h2 i {
      background: var(--gradient-primary);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      font-size: 32px;
      filter: drop-shadow(0 2px 6px rgba(0,177,79,0.3));
    }

    .btn-add-room {
      background: var(--gradient-primary);
      border: none;
      padding: 11px 24px;
      border-radius: 11px;
      color: white;
      font-weight: 700;
      font-size: 14px;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 4px 16px rgba(0,177,79,0.25);
      position: relative;
      overflow: hidden;
    }

    .btn-add-room::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      border-radius: 50%;
      background: rgba(255,255,255,0.3);
      transform: translate(-50%, -50%);
      transition: width 0.6s, height 0.6s;
    }

    .btn-add-room:hover::before {
      width: 300px;
      height: 300px;
    }

    .btn-add-room:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 24px rgba(0,177,79,0.35);
    }

    .btn-add-room:active {
      transform: translateY(-1px);
    }

    .btn-add-room i {
      margin-right: 8px;
    }

    .room-card {
      background: white;
      border-radius: 20px;
      box-shadow: 0 2px 20px rgba(0,0,0,0.08);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      overflow: hidden;
      margin-bottom: 24px;
      border: 1px solid rgba(0,0,0,0.05);
    }

    .room-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 12px 35px rgba(0,0,0,0.12);
      border-color: var(--primary-green);
    }

    .room-header {
      padding: 24px;
      color: white;
      position: relative;
      background: linear-gradient(135deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0.05) 100%);
    }

    .room-header.occupied {
      background: linear-gradient(135deg, #0d6efd 0%, #0056b3 100%);
    }

    .room-header.empty {
      background: linear-gradient(135deg, #6c757d 0%, #495057 100%);
    }

    .room-status-badge {
      position: absolute;
      top: 20px;
      right: 20px;
      background: rgba(255,255,255,0.25);
      backdrop-filter: blur(10px);
      padding: 6px 16px;
      border-radius: 25px;
      font-size: 11px;
      font-weight: 700;
      letter-spacing: 0.5px;
      border: 1px solid rgba(255,255,255,0.3);
    }

    .room-id {
      font-size: 26px;
      font-weight: 800;
      margin: 0;
      display: flex;
      align-items: center;
      gap: 12px;
      letter-spacing: -0.5px;
    }

    .room-body {
      padding: 24px;
    }

    .info-row {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 10px;
      padding: 12px 14px;
      background: #f8f9fa;
      border-radius: 10px;
      border-left: 3px solid var(--primary-green);
      transition: all 0.2s;
    }

    .info-row:hover {
      background: #e9ecef;
      transform: translateX(2px);
    }

    .info-row i {
      color: var(--primary-green);
      width: 22px;
      font-size: 16px;
      text-align: center;
    }

    .info-label {
      font-weight: 600;
      color: #6c757d;
      min-width: 90px;
      font-size: 13px;
    }

    .info-value {
      color: #212529;
      font-weight: 600;
      font-size: 14px;
      flex: 1;
    }

    .room-actions {
      display: flex;
      gap: 8px;
      padding-top: 20px;
      margin-top: 16px;
      border-top: 2px solid #e9ecef;
    }

    .btn-room-action {
      flex: 1;
      padding: 11px 8px;
      border: none;
      border-radius: 10px;
      font-weight: 600;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      cursor: pointer;
      font-size: 13px;
      letter-spacing: 0.3px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .btn-room-action:active {
      transform: scale(0.95);
    }

    .btn-assign {
      background: var(--primary-blue);
      color: white;
    }

    .btn-assign:hover {
      background: #003d7a;
      transform: translateY(-2px);
    }

    .btn-remove {
      background: #fd7e14;
      color: white;
    }

    .btn-remove:hover {
      background: #e56910;
      transform: translateY(-2px);
    }

    .btn-edit {
      background: #17a2b8;
      color: white;
    }

    .btn-edit:hover {
      background: #117a8b;
    }

    .btn-delete {
      background: var(--danger-red);
      color: white;
    }

    .btn-delete:hover {
      background: #c82333;
    }

    .modal {
      backdrop-filter: blur(5px);
    }

    .modal-content {
      border-radius: 15px;
      border: none;
      box-shadow: 0 10px 40px rgba(0,0,0,0.2);
    }

    .modal-header {
      background: linear-gradient(135deg, var(--primary-green) 0%, var(--secondary-teal) 100%);
      color: white;
      border-radius: 15px 15px 0 0;
      padding: 20px;
    }

    .modal-title {
      font-weight: 700;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .btn-close {
      filter: brightness(0) invert(1);
    }

    .form-label {
      font-weight: 600;
      color: #1a3a3a;
      margin-bottom: 8px;
    }

    .form-control, .form-select {
      border: 2px solid #e0e0e0;
      border-radius: 8px;
      padding: 10px 15px;
      transition: all 0.3s;
    }

    .form-control:focus, .form-select:focus {
      border-color: var(--primary-green);
      box-shadow: 0 0 0 0.2rem rgba(0,177,79,0.15);
    }

    .alert {
      border-radius: 10px;
      border: none;
      padding: 15px 20px;
      margin-bottom: 20px;
      animation: slideDown 0.3s ease;
    }

    @keyframes slideDown {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .empty-state {
      text-align: center;
      padding: 60px 20px;
      background: white;
      border-radius: 15px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    }

    .empty-state i {
      font-size: 64px;
      color: #ddd;
      margin-bottom: 20px;
    }

    .empty-state h3 {
      color: #666;
      margin-bottom: 10px;
    }

    .empty-state p {
      color: #999;
      margin-bottom: 25px;
    }
        @media (max-width: 768px) {
                .top-header {
                  padding: 0.8rem 0;
                }

                .header-content {
                  flex-direction: column;
                  gap: 12px;
                  text-align: center;
                }

                .logo-section {
                  gap: 10px;
                }

                .logo-section i {
                  font-size: 24px;
                }

                .logo-section h1 {
                  font-size: 18px;
                }

                .nav-section {
                  flex-wrap: wrap;
                  justify-content: center;
                  font-size: 13px;
                  gap: 12px;
                }

                /* Fixed spacing for mobile */
                .main-container {
                  padding-top: 140px !important;
                  padding-left: 15px;
                  padding-right: 15px;
                }

                .page-header {
                  margin-bottom: 20px;
                }

                .page-title {
                  font-size: 24px;
                }

                .page-subtitle {
                  font-size: 14px;
                }

                .stats-grid {
                  grid-template-columns: 1fr;
                  gap: 15px;
                }

                .stat-card {
                  padding: 15px;
                }

                .stat-number {
                  font-size: 28px;
                }

                .stat-icon {
                  font-size: 24px;
                }

                .metabase-iframe {
                  min-height: 500px;
                }

                .metabase-container {
                  min-height: 600px;
                }

                .data-body {
                  max-height: 400px;
                  padding: 10px;
                }

                .data-header {
                  padding: 15px;
                  flex-direction: column;
                  gap: 10px;
                  align-items: flex-start;
                }

                .data-header h3 {
                  font-size: 15px;
                }

                .data-table {
                  font-size: 12px;
                  display: block;
                  overflow-x: auto;
                  white-space: nowrap;
                }

                .data-table th,
                .data-table td {
                  padding: 8px 6px;
                }

                .tab-navigation {
                  flex-wrap: wrap;
                  gap: 10px;
                  margin-bottom: 20px;
                }

                .tab-button {
                  font-size: 14px;
                  padding: 10px 15px;
                  flex: 1;
                  min-width: 140px;
                }

                /* Fall Alert Mobile Styles */
                .fall-alert-container {
                  top: 145px;
                  left: 10px;
                  right: 10px;
                  max-width: none;
                  width: calc(100% - 20px);
                }

                .fall-alert {
                  padding: 15px;
                  border-width: 2px;
                  border-radius: 10px;
                  box-shadow: 0 8px 30px rgba(255, 0, 0, 0.35);
                }

                .fall-alert-header {
                  gap: 8px;
                  margin-bottom: 10px;
                  padding-bottom: 10px;
                }

                .fall-alert-icon {
                  font-size: 28px;
                  flex-shrink: 0;
                }

                .fall-alert-title {
                  font-size: 16px;
                  line-height: 1.2;
                }

                .fall-alert-body {
                  margin-bottom: 12px;
                }

                .fall-alert-info {
                  flex-direction: row;
                  justify-content: space-between;
                  gap: 8px;
                  align-items: center;
                  margin-bottom: 8px;
                  flex-wrap: wrap;
                }

                .fall-alert-label {
                  font-size: 12px;
                  flex-shrink: 0;
                }

                .fall-alert-value {
                  font-size: 13px;
                  text-align: right;
                  padding: 2px 6px;
                  word-break: break-word;
                }

                .fall-alert-footer {
                  display: grid;
                  grid-template-columns: 1fr 1fr;
                  gap: 8px;
                }

                .fall-alert-btn {
                  padding: 10px 8px;
                  font-size: 13px;
                  white-space: nowrap;
                }

                .fall-alert-timestamp {
                  font-size: 10px;
                  margin-top: 8px;
                  padding-top: 8px;
                }

                .info-box {
                  padding: 12px 15px;
                  font-size: 13px;
                }

                .empty-state {
                  padding: 30px 15px;
                }

                .empty-state i {
                  font-size: 36px;
                }

                /* Make tables scrollable on mobile */
                .data-container {
                  overflow-x: auto;
                }

                .badge-status {
                  font-size: 11px;
                  padding: 3px 8px;
                }

                .footer {
                  padding: 15px 10px;
                }

                .footer p {
                  font-size: 12px;
                }
              }

        /* Extra small devices */
        @media (max-width: 480px) {
          .top-header {
            padding: 0.6rem 0;
          }

          .logo-section i {
            font-size: 22px;
          }

          .logo-section h1 {
            font-size: 16px;
          }

          .nav-section {
            font-size: 12px;
            gap: 8px;
          }

          /* Extra padding for very small screens */
          .main-container {
            padding-top: 160px !important;
          }

          .page-title {
            font-size: 20px;
          }

          .stat-number {
            font-size: 24px;
          }

          .data-table {
            font-size: 11px;
          }

          .data-header h3 {
            font-size: 14px;
          }

          .tab-button {
            font-size: 13px;
            padding: 8px 10px;
            min-width: 100px;
          }

          /* Fall Alert for Extra Small Screens */
          .fall-alert-container {
            top: 165px;
            left: 8px;
            right: 8px;
            width: calc(100% - 16px);
          }

          .fall-alert {
            padding: 12px;
            border-radius: 8px;
          }

          .fall-alert-header {
            gap: 6px;
            margin-bottom: 8px;
            padding-bottom: 8px;
          }

          .fall-alert-icon {
            font-size: 24px;
          }

          .fall-alert-title {
            font-size: 14px;
          }

          .fall-alert-info {
            margin-bottom: 6px;
          }

          .fall-alert-label {
            font-size: 11px;
          }

          .fall-alert-value {
            font-size: 12px;
            padding: 2px 4px;
          }

          .fall-alert-footer {
            gap: 6px;
          }

          .fall-alert-btn {
            padding: 9px 6px;
            font-size: 12px;
          }

          .fall-alert-timestamp {
            font-size: 9px;
            margin-top: 6px;
            padding-top: 6px;
          }
        }

        /* Landscape mode adjustments */
        @media (max-height: 600px) and (orientation: landscape) {
          .fall-alert-container {
            top: 80px;
            max-height: 70vh;
            overflow-y: auto;
          }

          .fall-alert {
            margin-bottom: 10px;
          }

          .fall-alert-body {
            max-height: 150px;
            overflow-y: auto;
          }
        }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="top-header">
    <div class="container-fluid">
      <div class="d-flex justify-content-between align-items-center">
        <div class="logo-info">
          <i class="fas fa-hospital"></i>
          <div>
            <h1>Darsinurse Gateway</h1>
            <small class="text-muted">Manajemen Ruangan</small>
          </div>
        </div>
        <div class="user-info">
          <span><i class="fas fa-user-nurse"></i> <%= nama_perawat %></span>
          <span>|</span>
          <a href="/dashboard"><i class="fas fa-home"></i> Dashboard</a>
          <span>|</span>
          <a href="/logout" onclick="return confirm('Yakin logout?')">
            <i class="fas fa-sign-out-alt"></i> Logout
          </a>
        </div>
      </div>
    </div>
  </header>

  <!-- Main Content -->
  <div class="container-fluid main-container">
    <div class="page-title">
      <div class="d-flex justify-content-between align-items-center">
        <h2>
          <i class="fas fa-door-open"></i>
          Manajemen Ruangan
        </h2>
        <button class="btn btn-add-room" onclick="showAddRoomModal()">
          <i class="fas fa-plus"></i> Tambah Ruangan
        </button>
      </div>
    </div>

    <!-- Alert Messages -->
    <div id="alert-container"></div>

    <!-- Room Cards Container -->
    <div class="row" id="rooms-container">
      <div class="col-12">
        <div class="text-center">
          <div class="spinner-border text-primary" role="status">
            <span class="visually-hidden">Loading...</span>
          </div>
          <p class="mt-2">Memuat data ruangan...</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Modal: Add Room -->
  <div class="modal fade" id="addRoomModal" tabindex="-1">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">
            <i class="fas fa-plus-circle"></i>
            Tambah Ruangan Baru
          </h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <form id="form-add-room">
            <div class="mb-3">
              <label class="form-label">Room ID <span class="text-danger">*</span></label>
              <input type="text" class="form-control" id="add-room-id" placeholder="Contoh: EXECUTIVE_4" required>
              <small class="text-muted">ID unik untuk ruangan</small>
            </div>
            <div class="mb-3">
              <label class="form-label">Device ID <span class="text-danger">*</span></label>
              <input type="text" class="form-control" id="add-device-id" placeholder="Contoh: DEVICE_004" required>
              <small class="text-muted">ID perangkat monitoring</small>
            </div>
            <div class="mb-3">
              <label class="form-label">Pasien (Opsional)</label>
              <select class="form-select" id="add-emr-no">
                <option value="">-- Ruangan Kosong --</option>
              </select>
              <small class="text-muted">Bisa diisi kemudian</small>
            </div>
            <div class="d-grid gap-2">
              <button type="submit" class="btn btn-success">
                <i class="fas fa-save"></i> Simpan Ruangan
              </button>
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                <i class="fas fa-times"></i> Batal
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- Modal: Edit Room -->
  <div class="modal fade" id="editRoomModal" tabindex="-1">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">
            <i class="fas fa-edit"></i>
            Edit Ruangan
          </h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <form id="form-edit-room">
            <input type="hidden" id="edit-old-room-id">
            <div class="mb-3">
              <label class="form-label">Room ID <span class="text-danger">*</span></label>
              <input type="text" class="form-control" id="edit-room-id" required>
            </div>
            <div class="mb-3">
              <label class="form-label">Device ID <span class="text-danger">*</span></label>
              <input type="text" class="form-control" id="edit-device-id" required>
            </div>
            <div class="d-grid gap-2">
              <button type="submit" class="btn btn-primary">
                <i class="fas fa-save"></i> Update Ruangan
              </button>
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                <i class="fas fa-times"></i> Batal
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- Modal: Assign Patient -->
  <div class="modal fade" id="assignPatientModal" tabindex="-1">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">
            <i class="fas fa-user-plus"></i>
            Masukkan Pasien ke Ruangan
          </h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <div class="alert alert-info">
            <i class="fas fa-info-circle"></i>
            Ruangan: <strong id="assign-room-display"></strong>
          </div>
          <form id="form-assign-patient">
            <input type="hidden" id="assign-room-id">
            <div class="mb-3">
              <label class="form-label">Pilih Pasien <span class="text-danger">*</span></label>
              <select class="form-select" id="assign-emr-no" required>
                <option value="">-- Pilih Pasien --</option>
              </select>
              <small class="text-muted">Hanya menampilkan pasien yang belum ada di ruangan</small>
            </div>
            <div class="d-grid gap-2">
              <button type="submit" class="btn btn-success">
                <i class="fas fa-check"></i> Masukkan Pasien
              </button>
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                <i class="fas fa-times"></i> Batal
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // ============================================
    // GLOBAL VARIABLES
    // ============================================
    let rooms = [];
    let availablePatients = [];
    const addRoomModal = new bootstrap.Modal(document.getElementById('addRoomModal'));
    const editRoomModal = new bootstrap.Modal(document.getElementById('editRoomModal'));
    const assignPatientModal = new bootstrap.Modal(document.getElementById('assignPatientModal'));

    // ============================================
    // LOAD DATA
    // ============================================
    async function loadRooms() {
      try {
        const response = await fetch('/api/rooms');
        const result = await response.json();
        
        if (result.success) {
          rooms = result.rooms;
          renderRooms();
        } else {
          showAlert('danger', 'Gagal memuat data ruangan');
        }
      } catch (error) {
        showAlert('danger', 'Error: ' + error.message);
      }
    }

    async function loadAvailablePatients() {
      try {
        const response = await fetch('/api/rooms/available-patients');
        const result = await response.json();
        
        if (result.success) {
          availablePatients = result.patients;
          populatePatientDropdowns();
        }
      } catch (error) {
        console.error('Error loading patients:', error);
      }
    }

    function populatePatientDropdowns() {
      const addSelect = document.getElementById('add-emr-no');
      const assignSelect = document.getElementById('assign-emr-no');
      
      const options = availablePatients.map(p => 
        `<option value="${p.emr_no}">${p.nama} (EMR: ${p.emr_no}) - ${p.poli}</option>`
      ).join('');
      
      addSelect.innerHTML = '<option value="">-- Ruangan Kosong --</option>' + options;
      assignSelect.innerHTML = '<option value="">-- Pilih Pasien --</option>' + options;
    }

    // ============================================
    // RENDER ROOMS
    // ============================================
    function renderRooms() {
      const container = document.getElementById('rooms-container');
      
      if (rooms.length === 0) {
        container.innerHTML = `
          <div class="col-12">
            <div class="empty-state">
              <i class="fas fa-door-open"></i>
              <h3>Belum Ada Ruangan</h3>
              <p>Mulai dengan menambahkan ruangan baru</p>
              <button class="btn btn-add-room" onclick="showAddRoomModal()">
                <i class="fas fa-plus"></i> Tambah Ruangan Pertama
              </button>
            </div>
          </div>
        `;
        return;
      }

      const html = rooms.map(room => {
        const isOccupied = room.emr_no !== null;
        const statusClass = isOccupied ? 'occupied' : 'empty';
        const statusText = isOccupied ? 'TERISI' : 'KOSONG';
        const icon = isOccupied ? 'fa-door-closed' : 'fa-door-open';
        
        return `
          <div class="col-md-6 col-lg-4">
            <div class="room-card">
              <div class="room-header ${statusClass}">
                <span class="room-status-badge">${statusText}</span>
                <h3 class="room-id">
                  <i class="fas ${icon}"></i>
                  ${room.room_id}
                </h3>
              </div>
              <div class="room-body">
                <div class="info-row">
                  <i class="fas fa-microchip"></i>
                  <span class="info-label">Device ID:</span>
                  <code class="info-value">${room.device_id}</code>
                </div>
                
                ${isOccupied ? `
                  <div class="info-row">
                    <i class="fas fa-user"></i>
                    <span class="info-label">Pasien:</span>
                    <span class="info-value">${room.nama_pasien}</span>
                  </div>
                  <div class="info-row">
                    <i class="fas fa-id-card"></i>
                    <span class="info-label">EMR:</span>
                    <span class="info-value">${room.emr_no}</span>
                  </div>
                  <div class="info-row">
                    <i class="fas fa-hospital"></i>
                    <span class="info-label">Poli:</span>
                    <span class="info-value">${room.poli}</span>
                  </div>
                  <div class="info-row">
                    <i class="fas fa-clock"></i>
                    <span class="info-label">Masuk:</span>
                    <span class="info-value">${formatDate(room.assigned_at)}</span>
                  </div>
                ` : `
                  <div class="alert alert-secondary mb-0">
                    <i class="fas fa-info-circle"></i> Ruangan masih kosong
                  </div>
                `}
                
                <div class="room-actions">
                  ${isOccupied ? `
                    <button class="btn-room-action btn-remove" onclick="removePatient('${room.room_id}')">
                      <i class="fas fa-door-open"></i> Keluarkan
                    </button>
                  ` : `
                    <button class="btn-room-action btn-assign" onclick="showAssignModal('${room.room_id}')">
                      <i class="fas fa-user-plus"></i> Masukkan Pasien
                    </button>
                  `}
                  <button class="btn-room-action btn-edit" onclick="showEditModal('${room.room_id}', '${room.device_id}')">
                    <i class="fas fa-edit"></i>
                  </button>
                  <button class="btn-room-action btn-delete" onclick="deleteRoom('${room.room_id}')">
                    <i class="fas fa-trash"></i>
                  </button>
                </div>
              </div>
            </div>
          </div>
        `;
      }).join('');
      
      container.innerHTML = html;
    }

    // ============================================
    // MODAL FUNCTIONS
    // ============================================
    function showAddRoomModal() {
      document.getElementById('form-add-room').reset();
      loadAvailablePatients();
      addRoomModal.show();
    }

    function showEditModal(roomId, deviceId) {
      document.getElementById('edit-old-room-id').value = roomId;
      document.getElementById('edit-room-id').value = roomId;
      document.getElementById('edit-device-id').value = deviceId;
      editRoomModal.show();
    }

    function showAssignModal(roomId) {
      document.getElementById('assign-room-id').value = roomId;
      document.getElementById('assign-room-display').textContent = roomId;
      loadAvailablePatients();
      assignPatientModal.show();
    }

    // ============================================
    // FORM HANDLERS
    // ============================================
    document.getElementById('form-add-room').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const roomId = document.getElementById('add-room-id').value.trim();
      const deviceId = document.getElementById('add-device-id').value.trim();
      const emrNo = document.getElementById('add-emr-no').value;
      
      try {
        const response = await fetch('/api/rooms/add', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            room_id: roomId,
            device_id: deviceId,
            emr_no: emrNo || null
          })
        });
        
        const result = await response.json();
        
        if (result.success) {
          showAlert('success', result.message);
          addRoomModal.hide();
          loadRooms();
          loadAvailablePatients();
        } else {
          showAlert('danger', result.error);
        }
      } catch (error) {
        showAlert('danger', 'Error: ' + error.message);
      }
    });

    document.getElementById('form-edit-room').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const oldRoomId = document.getElementById('edit-old-room-id').value;
      const newRoomId = document.getElementById('edit-room-id').value.trim();
      const deviceId = document.getElementById('edit-device-id').value.trim();
      
      try {
        const response = await fetch(`/api/rooms/${oldRoomId}`, {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            new_room_id: newRoomId,
            device_id: deviceId
          })
        });
        
        const result = await response.json();
        
        if (result.success) {
          showAlert('success', result.message);
          editRoomModal.hide();
          loadRooms();
        } else {
          showAlert('danger', result.error);
        }
      } catch (error) {
        showAlert('danger', 'Error: ' + error.message);
      }
    });

    document.getElementById('form-assign-patient').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const roomId = document.getElementById('assign-room-id').value;
      const emrNo = document.getElementById('assign-emr-no').value;
      
      if (!emrNo) {
        showAlert('warning', 'Silakan pilih pasien');
        return;
      }
      
      try {
        const response = await fetch('/api/rooms/assign', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            room_id: roomId,
            emr_no: parseInt(emrNo)
          })
        });
        
        const result = await response.json();
        
        if (result.success) {
          showAlert('success', result.message);
          assignPatientModal.hide();
          loadRooms();
          loadAvailablePatients();
        } else {
          showAlert('danger', result.error);
        }
      } catch (error) {
        showAlert('danger', 'Error: ' + error.message);
      }
    });

    // ============================================
    // ACTION FUNCTIONS
    // ============================================
    async function removePatient(roomId) {
      if (!confirm(`Apakah Anda yakin ingin mengeluarkan pasien dari ruangan ${roomId}?`)) {
        return;
      }
      
      try {
        const response = await fetch('/api/rooms/remove-patient', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ room_id: roomId })
        });
        
        const result = await response.json();
        
        if (result.success) {
          showAlert('success', result.message);
          loadRooms();
          loadAvailablePatients();
        } else {
          showAlert('danger', result.error);
        }
      } catch (error) {
        showAlert('danger', 'Error: ' + error.message);
      }
    }

    async function deleteRoom(roomId) {
      if (!confirm(`Apakah Anda yakin ingin menghapus ruangan ${roomId}?\n\nRuangan harus kosong untuk dihapus.`)) {
        return;
      }
      
      try {
        const response = await fetch('/api/rooms/delete', {
          method: 'DELETE',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ room_id: roomId })
        });
        
        const result = await response.json();
        
        if (result.success) {
          showAlert('success', result.message);
          loadRooms();
        } else {
          showAlert('danger', result.error);
        }
      } catch (error) {
        showAlert('danger', 'Error: ' + error.message);
      }
    }

    // ============================================
    // UTILITY FUNCTIONS
    // ============================================
    function showAlert(type, message) {
      const container = document.getElementById('alert-container');
      const alertDiv = document.createElement('div');
      alertDiv.className = `alert alert-${type} alert-dismissible fade show`;
      alertDiv.innerHTML = `
        <i class="fas fa-${type === 'success' ? 'check-circle' : type === 'danger' ? 'exclamation-circle' : 'info-circle'}"></i>
        ${message}
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      `;
      
      container.appendChild(alertDiv);
      
      setTimeout(() => {
        alertDiv.remove();
      }, 5000);
    }

    function formatDate(dateString) {
      if (!dateString) return '-';
      const date = new Date(dateString);
      return date.toLocaleString('id-ID', {
        day: '2-digit',
        month: 'short',
        year: 'numeric',
        hour: '2-digit',
        minute: '2-digit'
      });
    }

    // ============================================
    // INITIALIZE ON PAGE LOAD
    // ============================================
    document.addEventListener('DOMContentLoaded', function() {
      console.log('✓ Room Management Page Loaded');
      loadRooms();
      loadAvailablePatients();
    });
  </script>
</body>
</html>